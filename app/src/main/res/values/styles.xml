<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorControlNormal">@color/red400</item>
        <item name="colorControlActivated">@color/redPrimary</item>
        <item name="colorControlHighlight">@color/redPrimaryDark</item>
    </style>

</resources>
